# RxJS Observables-Fortified ReactJS & Socket.io Chat 

**a testing environment**

stack used:

*reactjs
*immutable.js
*rxjs
*socket.io
*nodejs & express
*materialize


setup:

$ npm install webpack -g

$ npm install

$ webpack

$ node server.js

open browser to `localhost:5002`